<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="sd.gtlogin">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">

        <activity
            android:name="com.udacity.android.grschroe.GeorgiaTechSSOActivity"
            android:label="GT SSO Login" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
